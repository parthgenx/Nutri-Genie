<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | Nutri-Genie</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .glass {
            background: rgba(255, 255, 255, 0.85);
            backdrop-filter: blur(12px);
        }
    </style>
</head>
<body class="bg-gradient-to-br from-teal-50 to-blue-100 min-h-screen flex items-center justify-center font-sans text-gray-800">

    <div class="glass p-10 rounded-2xl shadow-2xl w-full max-w-md border border-white/60 relative overflow-hidden">
        
        <div class="absolute top-0 left-0 w-full h-2 bg-gradient-to-r from-teal-400 to-blue-500"></div>

        <div class="text-center mb-8">
            <div class="text-5xl mb-4">🔐</div>
            <h2 class="text-3xl font-extrabold text-teal-800">Welcome Back</h2>
            <p class="text-gray-500 mt-2">Enter your credentials to access your dashboard.</p>
        </div>
        
        <form action="/login" method="POST" class="space-y-6">
            
            <div>
                <label for="username" class="block text-sm font-bold text-gray-700 mb-1 ml-1">Username</label>
                <input type="text" id="username" name="username" required 
                    class="w-full px-4 py-3 rounded-xl border border-gray-200 focus:ring-2 focus:ring-teal-500 focus:border-transparent outline-none transition bg-white/70 placeholder-gray-400"
                    placeholder="e.g. health_hero">
            </div>

            <div>
                <label for="password" class="block text-sm font-bold text-gray-700 mb-1 ml-1">Password</label>
                <input type="password" id="password" name="password" required 
                    class="w-full px-4 py-3 rounded-xl border border-gray-200 focus:ring-2 focus:ring-teal-500 focus:border-transparent outline-none transition bg-white/70 placeholder-gray-400"
                    placeholder="••••••••">
            </div>

            <button type="submit" 
                class="w-full bg-gradient-to-r from-teal-600 to-teal-700 hover:from-teal-700 hover:to-teal-800 text-white font-bold py-3.5 px-4 rounded-xl shadow-lg transform transition hover:scale-[1.02] focus:ring-2 focus:ring-offset-2 focus:ring-teal-500">
                Sign In
            </button>
            
        </form>

        <div class="mt-8 text-center text-sm text-gray-500">
            First time here? <a href="/new" class="text-teal-600 font-bold hover:underline">Start your journey</a>
        </div>

    </div>

</body>
</html>