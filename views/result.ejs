<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Health Plan | Nutri-Genie</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .glass {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
        }
        /* Basic styling for the AI's HTML output */
        .ai-content h3 { font-size: 1.5rem; font-weight: bold; color: #0f766e; margin-top: 1.5rem; margin-bottom: 0.5rem; }
        .ai-content ul { list-style-type: disc; padding-left: 1.5rem; margin-bottom: 1rem; }
        .ai-content p { margin-bottom: 1rem; line-height: 1.6; }
        .ai-content strong { color: #0d9488; }
    </style>
</head>
<body class="bg-gradient-to-br from-teal-50 to-blue-100 min-h-screen font-sans text-gray-800">

    <div class="container mx-auto px-4 py-10 max-w-4xl">
        <div class="text-center mb-8">
            <h1 class="text-3xl font-bold text-teal-700">🥗 Your Personalized Plan</h1>
            <p class="text-gray-600">Generated by Gemini AI</p>
        </div>

        <div class="glass rounded-2xl shadow-xl p-8 border border-white/50">
            
            <div class="ai-content text-gray-700">
                <%- report %>
            </div>

            <div class="mt-10 pt-6 border-t border-gray-200 flex justify-between items-center">
                <a href="/" class="text-teal-600 hover:text-teal-800 font-semibold flex items-center gap-2 transition">
                    ← Generate Another
                </a>
                <button onclick="window.print()" class="bg-teal-600 hover:bg-teal-700 text-white px-6 py-2 rounded-lg shadow hover:scale-105 transition">
                    🖨️ Print / Save PDF
                </button>
            </div>

        </div>
    </div>

</body>
</html>